/*
 *  Copyright 2022 Imandana Rahimaswara
 *
 * Released under the MIT license. See the accompanying LICENSE file
 * or visit <https://opensource.org/licenses/MIT>
 */
 
#include <stdio.h>

#include "dynamicarray.h"

/* unsigned int modulo_func2 (unsigned int count)
{
   if (++count >= 60)
  count = 0;
  return (count);
}

int Data(int d)
{
	printf("%d \n", d);
	
	return d;
} */

int main()
{
	
/* 	if(Data(4) > 3 && Data(4) < 5 && Data(4) == 1)
	{
		return 0;
	}
	 */
	 
	 DynamicArray arrayOfInt;
	 arrayOfInt.blockCount = 5;
	 arrayOfInt.size = 0;
	 
	return 0;
}
