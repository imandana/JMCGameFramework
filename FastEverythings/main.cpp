#include <iostream>

#include "fasteasing.h"

int main()
{
	float curPos = 0;
	int toPos = 40;
/* 	
	for(int i = 0; i < 20; i++)
	{
		curPos += EasingLinear(i, toPos);
		
		std::cout << curPos << std::endl;
	}
	 */
	
/* 	for(int i = 0; i < toPos; i++)
	{
		curPos = EasingSine(i , toPos)  * toPos;
		
		std::cout << curPos << std::endl;
	}
	 */
	 
	 
	return 0;
}
